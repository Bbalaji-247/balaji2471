git is a command
we can edit modify commit files in git
in git workspace staging ares git repo will present
sto move from workspace to staging area we use git add
from staging area to local repo we use  git commit -m "comment"

